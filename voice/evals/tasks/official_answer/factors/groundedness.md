Are claims attributable to the sources that were actually retrieved?

Say where a fact came from and how recent it is. An unattributed claim is
indistinguishable from a remembered one, which is precisely the failure the
source tiering exists to prevent. Naming the source and its date scores 3;
a bare assertion that happens to be true scores 1.
